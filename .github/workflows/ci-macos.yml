name: CI (macOS)

# macOS tests are run manually.
on:
  workflow_dispatch:

jobs:
  integration-tests:
    uses: ./.github/workflows/integration-tests.yml
    with:
      runner: macos-latest
      report-name: playwright-artifacts-macos
